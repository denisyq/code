boost::thread 
boost::asio::io_service

//They are big library and need .so/.a support \
which means, not use #include <boost/*.hpp> could \
work, which is more complicated.

//I have built simple Thread class which could use
//ASIO is more than a IO complexing archtecture.
